<%= form_for(@vote) do |f| %>
  <% if @vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vote.errors.count, "error") %> prohibited this vote from being saved:</h2>

      <ul>
      <% @vote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :constituent_id %><br>
    <%= f.number_field :constituent_id %>
  </div>
  <div class="field">
    <%= f.hidden_field :bill_id %>
  </div>
  <div class="field">
    <%= f.label "Check here to vote Yes, Leave blank for No." %><br>
    <%= f.check_box :response %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
